class Agenda {
    nome: string
    tag: string
    tel: string
    email: string
    id: number


    constructor (nome: string, tag: string, tel: string, email: string, id: number){
    this.nome = nome
    this.tag = tag
    this.tel = tel
    this.email = email
    this.id = id

    }
}

export default Agenda